import React from 'react';

export namespace GraphMainSelection {
    export interface Props {
    }
}

export const GraphMainSelection = (props: GraphMainSelection.Props) => {
    return (
        <div>selection</div>
    );
};
