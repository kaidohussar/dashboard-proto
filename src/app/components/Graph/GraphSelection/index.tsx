import React from 'react';

export namespace GraphSelection {
    export interface Props {
    }
}

export const GraphSelection = (props: GraphSelection.Props) => {
    return (
        <div>asd</div>
    );
};
